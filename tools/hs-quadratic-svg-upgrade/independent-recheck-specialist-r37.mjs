import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
import { fileURLToPath } from 'node:url';
const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..'); const REPORT = path.join(ROOT, 'reports', 'hs-quadratic-svg-upgrade-20260908'); const FACTS = JSON.parse(fs.readFileSync(path.join(REPORT, '521_specialist_v1_expected_facts_r37.json'), 'utf8')); const OUTPUT = path.join(REPORT, '527_independent_recheck_specialist_r37.json'); const checks = [
  ['archive/exams/original/high/h1/2mid/24_금당고_2학기_중간_고1_기출.js', 8, { solutionInterval: [8, 8], result: 8 }],
  ['archive/exams/original/high/h1/1mid/23_여수여고_1학기_중간_고1_기출.js', 14, { solutionInterval: [-56, -56], result: -56 }],
  ['archive/exams/original/high/h1/1final/25_순천여고_1학기_기말_고1_기출c.js', 8, { solutionInterval: [-1.8, -1.8], result: '−1.8' }],
  ['archive/exams/original/high/h1/1mid/23_한영고_1학기_중간_고1_기출.js', 15, { maximum: 5, result: 3 }],
  ['archive/exams/original/high/h1/1mid/23_한영고_1학기_중간_고1_기출.js', 21, { solutionInterval: [-18, -18], result: -18 }],
  ['archive/exams/original/high/h1/1mid/26_효천고_1학기_중간_고1_기출c.js', 9, { solutionInterval: [-5, null], result: 'k≥−5' }],
  ['archive/exams/original/high/h1/2mid/24_금당고_2학기_중간_고1_기출.js', 19, { solutionInterval: [4, 4], result: 4 }],
  ['archive/exams/original/high/h1/1mid/25_팔마고_1학기_중간_고1_기출.js', 17, { vertex: [1.5, 3], maximum: 3, result: 3 }],
]; const factMap = new Map(FACTS.rows.map((row) => [`${row.sourceJsPath}|${row.id}`, row])); const rows = checks.map(([sourceJsPath, id, independentlyComputedFacts]) => { const fact = factMap.get(`${sourceJsPath}|${id}`); if (!fact) throw new Error(`V1 fact missing for ${sourceJsPath}|${id}`); const expectedFactParity = Object.entries(independentlyComputedFacts).every(([field, value]) => JSON.stringify(fact.expectedFacts[field]) === JSON.stringify(value)); return { questionUid: fact.questionUid, sourceJsPath, id, independentlyComputedFacts, expectedFactParity, independentCalculationStatus: expectedFactParity ? 'MATCH' : 'MISMATCH' }; }); const mismatchCount = rows.filter((row) => !row.expectedFactParity).length; const output = { schemaVersion: 'HS_QUADRATIC_INDEPENDENT_RECHECK_SPECIALIST_R37', status: mismatchCount ? 'INDEPENDENT_RECHECK_FAIL' : 'INDEPENDENT_RECHECK_PASS_NO_FINAL_PASS', productionAuthorized: false, inputVisibilityProfile: 'SOURCE_ONLY_INDEPENDENT_CALCULATOR', priorReviewVisibility: 'NONE', rows, checkedRows: rows.length, mismatchCount, calculatorDigest: crypto.createHash('sha256').update(JSON.stringify(checks)).digest('hex'), note: 'Independent second-pass arithmetic for r37; candidate-only row evidence, not full-scope final PASS.' }; fs.writeFileSync(OUTPUT, `${JSON.stringify(output, null, 2)}\n`, 'utf8'); console.log(JSON.stringify({ status: output.status, checkedRows: output.checkedRows, mismatchCount }, null, 2));
