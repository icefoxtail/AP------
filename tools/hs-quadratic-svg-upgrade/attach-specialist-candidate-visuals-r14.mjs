import fs from 'node:fs'; import path from 'node:path'; import vm from 'node:vm'; import crypto from 'node:crypto'; import { fileURLToPath } from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const REPORT=path.join(ROOT,'reports','hs-quadratic-svg-upgrade-20260908');const BASE=JSON.parse(fs.readFileSync(path.join(REPORT,'103_specialist_candidate_bank_manifest_r13.json'),'utf8'));const FACTS=JSON.parse(fs.readFileSync(path.join(REPORT,'118_specialist_v1_expected_facts_r14.json'),'utf8'));const VISUALS=JSON.parse(fs.readFileSync(path.join(REPORT,'119_specialist_candidate_visual_manifest_r14.json'),'utf8'));const OUT=path.join(ROOT,'archive','_generated','hs-quadratic-svg-upgrade-20260908','candidate-r14','exams');const OUTPUT=path.join(REPORT,'120_specialist_candidate_bank_manifest_r14.json');
function load(relative){const c={window:{}};vm.createContext(c);vm.runInContext(fs.readFileSync(path.join(ROOT,relative),'utf8'),c,{filename:relative,timeout:10000});return JSON.parse(JSON.stringify(c.window));}function sha(value){return crypto.createHash('sha256').update(value).digest('hex');}function key(sourcePath,id){return `${sourcePath}|${id}`;}function protectedHash(q){return sha(JSON.stringify({id:q.id??null,content:q.content??null,choices:q.choices??null,answer:q.answer??null,image:q.image??null}));}
const factByKey=new Map(FACTS.rows.map(row=>[key(row.sourceJsPath,row.id),row]));const visualByKey=new Map(VISUALS.rows.map(row=>[key(row.questionUid.split('|')[0],row.questionUid.split('|').at(-1)),row]));const rows=[];const errors=[];const candidateFiles=[];let actualVisualCount=0;fs.mkdirSync(OUT,{recursive:true});
for(let index=0;index<BASE.candidateFiles.length;index+=1){const file=BASE.candidateFiles[index];const source=load(file.sourcePath);const candidate=load(file.candidatePath);for(const question of candidate.questionBank){const visual=visualByKey.get(key(file.sourcePath,question.id));if(!visual)continue;const fact=factByKey.get(key(file.sourcePath,question.id));if(!fact){errors.push(`FACT_MISSING:${file.sourcePath}|${question.id}`);continue;}const before=protectedHash(question);question.solutionImage=visual.assetPath;question.solutionImageAlt='이차함수·이차부등식의 핵심 조건과 해집합을 확인하는 후보 해설 시각자료';question.solutionImageCaption='후보 SVG: 독립 계산으로 확정한 구간·끝점·꼭짓점·핵심 값을 그림에서 확인한다.';question.solutionImageSize='full';if(before!==protectedHash(question))errors.push(`PROTECTED_DRIFT:${file.sourcePath}|${question.id}`);rows.push({questionUid:fact.questionUid,sourceJsPath:file.sourcePath,id:Number(question.id),caseId:visual.caseId,assetPath:visual.assetPath,protectedHashBefore:before,protectedHashAfter:protectedHash(question),status:'SPECIALIST_CANDIDATE_VISUAL_BOUND_NO_PASS'});}actualVisualCount+=candidate.questionBank.filter(question=>question.solutionImage).length;const outputName=`${String(index+1).padStart(3,'0')}-${path.basename(file.sourcePath)}`;const outputPath=path.join(OUT,outputName);fs.writeFileSync(outputPath,`window.examTitle = ${JSON.stringify(source.examTitle)};\nwindow.questionBank = ${JSON.stringify(candidate.questionBank,null,2)};\n`,'utf8');candidateFiles.push({sourcePath:file.sourcePath,candidatePath:`archive/_generated/hs-quadratic-svg-upgrade-20260908/candidate-r14/exams/${outputName}`,sourceQuestionCount:source.questionBank.length,candidateQuestionCount:candidate.questionBank.length,candidateFileSha256:sha(fs.readFileSync(outputPath))});}
if(rows.length!==FACTS.rows.length)errors.push(`ROW_COUNT:${rows.length}`);if(new Set(rows.map(row=>key(row.sourceJsPath,row.id))).size!==rows.length)errors.push('DUPLICATE_SPECIALIST_VISUAL_ROWS');const supersededInheritedVisualCount=BASE.totalDeclaredCandidateVisualCount+rows.length-actualVisualCount;const output={schemaVersion:'HS_QUADRATIC_SPECIALIST_CANDIDATE_BANK_MANIFEST_R14',status:errors.length?'SPECIALIST_CANDIDATE_BANK_FAIL':'SPECIALIST_CANDIDATE_BANK_ATTACHED_NO_PASS',productionAuthorized:false,baseCandidateManifest:'reports/hs-quadratic-svg-upgrade-20260908/103_specialist_candidate_bank_manifest_r13.json',v1Facts:'reports/hs-quadratic-svg-upgrade-20260908/118_specialist_v1_expected_facts_r14.json',visualManifest:'reports/hs-quadratic-svg-upgrade-20260908/119_specialist_candidate_visual_manifest_r14.json',targetQuestionCount:430,candidateFileCount:candidateFiles.length,inheritedVisualCount:BASE.totalDeclaredCandidateVisualCount,attachedSpecialistVisualCount:rows.length,supersededInheritedVisualCount,totalDeclaredCandidateVisualCount:actualVisualCount,candidateFiles,rows,errors,note:'Candidate-r14 carries the validated r13 bank and adds 18 fresh specialist candidate visuals. Production source/assets and final PASS remain untouched.'};fs.writeFileSync(OUTPUT,`${JSON.stringify(output,null,2)}\n`,'utf8');console.log(JSON.stringify({status:output.status,candidateFileCount:output.candidateFileCount,attachedSpecialistVisualCount:output.attachedSpecialistVisualCount,supersededInheritedVisualCount:output.supersededInheritedVisualCount,totalDeclaredCandidateVisualCount:output.totalDeclaredCandidateVisualCount,errors:output.errors.length},null,2));
