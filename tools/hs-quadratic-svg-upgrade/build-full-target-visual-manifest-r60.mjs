import fs from 'node:fs'; import path from 'node:path'; import vm from 'node:vm'; import crypto from 'node:crypto'; import { fileURLToPath } from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..'),REPORT=path.join(ROOT,'reports','hs-quadratic-svg-upgrade-20260908'),SCOPED=JSON.parse(fs.readFileSync(path.join(REPORT,'735_scoped_candidate_bank_manifest_r49.json'))),TRIAGE=fs.readFileSync(path.join(REPORT,'03_visual_triage.jsonl'),'utf8').trim().split(/\r?\n/).map(JSON.parse),OUTPUT=path.join(REPORT,'762_full_target_visual_manifest_r60.json');
const decisions=new Map(TRIAGE.map(row=>[`${row.questionUid.split('|')[0]}|${row.questionUid.split('|').at(-1)}`,row]));function load(relativePath){const context={window:{}};vm.createContext(context);vm.runInContext(fs.readFileSync(path.join(ROOT,relativePath),'utf8'),context,{filename:relativePath,timeout:10000});return JSON.parse(JSON.stringify(context.window));}
const rows=[];for(const file of SCOPED.candidateFiles){const bank=load(file.candidatePath);for(const question of bank.questionBank){const decision=decisions.get(`${file.sourcePath}|${question.id}`);if(!question.solutionImage||decision?.visualDecision==='NO_VISUAL')continue;const assetPath=question.solutionImage;const svg=fs.readFileSync(path.join(ROOT,assetPath),'utf8');const caseMatch=svg.match(/data-visual-case="([^"]+)"/);rows.push({questionUid:`${file.sourcePath}|${bank.examTitle}|${question.id}`,sourcePath:file.sourcePath,id:Number(question.id),visualDecision:decision?.visualDecision??null,caseId:caseMatch?.[1]??null,assetPath,assetSha256:crypto.createHash('sha256').update(svg).digest('hex'),solutionImageAlt:question.solutionImageAlt??null});}}
const missingCaseIds=rows.filter(row=>!row.caseId).length,output={schemaVersion:'HS_QUADRATIC_FULL_TARGET_VISUAL_MANIFEST_R60',status:rows.length===379&&missingCaseIds===0?'FULL_TARGET_VISUAL_MANIFEST_READY_NO_PASS':'FULL_TARGET_VISUAL_MANIFEST_REVIEW_REQUIRED',productionAuthorized:false,targetRows:430,targetVisualRows:rows.length,missingCaseIds,rows,sourceOfTruth:{scopedCandidate:'reports/hs-quadratic-svg-upgrade-20260908/735_scoped_candidate_bank_manifest_r49.json',visualCoverage:'reports/hs-quadratic-svg-upgrade-20260908/740_target_scoped_visual_coverage_r49.json'},note:'Aggregates all 379 target-scoped solutionImage assets for full local desktop/mobile render. No provider-attested PASS is claimed.'};fs.writeFileSync(OUTPUT,`${JSON.stringify(output,null,2)}\n`);console.log(JSON.stringify({status:output.status,targetRows:output.targetRows,targetVisualRows:output.targetVisualRows,missingCaseIds},null,2));
