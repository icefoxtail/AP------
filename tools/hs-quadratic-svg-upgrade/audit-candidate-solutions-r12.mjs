import fs from 'node:fs'; import path from 'node:path'; import vm from 'node:vm'; import { fileURLToPath } from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..'); const REPORT=path.join(ROOT,'reports','hs-quadratic-svg-upgrade-20260908'); const BANKS=JSON.parse(fs.readFileSync(path.join(REPORT,'83_specialist_candidate_bank_manifest_r12.json'),'utf8')); const OUTPUT=path.join(REPORT,'99_candidate_solution_static_audit_r12.json');
function load(relative){const c={window:{}};vm.createContext(c);vm.runInContext(fs.readFileSync(path.join(ROOT,relative),'utf8'),c,{filename:relative,timeout:10000});return c.window.questionBank||[];} function directionFlags(solution){const out=[];for(const match of String(solution||'').matchAll(/이차항의\s*계수가\s*(양수|음수)[^\n.]{0,30}?(위로|아래로)\s*(?:볼록|열린)/g)){if((match[1]==='양수')!==(match[2]==='위로'))out.push('PARABOLA_DIRECTION_WORDING_CONTRADICTION');}return out;}
const rows=[];const issues=[];for(const file of BANKS.candidateFiles){const bank=load(file.candidatePath);for(const q of bank){const solution=String(q.solution||'');const generic=/^\s*주어진 식 또는 그래프에서[\s\S]*주어진 정답과 일치하는/.test(solution);const stale=/원문\s*(?:표시\s*)?정답[^.\n]{0,80}(?:충돌|다르|불일치)/.test(solution);const direction=directionFlags(solution);const codes=[...(generic?['GENERIC_SOLUTION_PLACEHOLDER']:[]),...(stale?['STALE_SOURCE_ANSWER_CONFLICT']:[]),...direction];const row={sourceJsPath:file.sourcePath,id:Number(q.id),genericPlaceholder:generic,staleAnswerConflict:stale,directionFlags:direction,codes,status:codes.length?'REVIEW_REQUIRED':'CANDIDATE_SOLUTION_STATIC_OK'};rows.push(row);if(codes.length)issues.push(row);}}
const output={schemaVersion:'HS_QUADRATIC_CANDIDATE_SOLUTION_STATIC_AUDIT_R12',status:issues.length?'REVIEW_REQUIRED':'CANDIDATE_SOLUTION_STATIC_OK_NO_MATH_PASS',productionAuthorized:false,sourceFileCount:BANKS.candidateFiles.length,candidateBankQuestionCount:rows.length,counts:{genericSolutionPlaceholderRows:rows.filter(row=>row.genericPlaceholder).length,staleAnswerConflictRows:rows.filter(row=>row.staleAnswerConflict).length,directionContradictionRows:rows.filter(row=>row.directionFlags.length).length,issueRows:issues.length},issues,rows,note:'Candidate-r12 solution scan proves that prior candidate solution repairs removed the known generic/stale/direction residuals from the candidate banks. It does not replace independent A1/A2 review or provider final audit.'};fs.writeFileSync(OUTPUT,`${JSON.stringify(output,null,2)}\n`,'utf8');console.log(JSON.stringify({status:output.status,sourceFileCount:output.sourceFileCount,candidateBankQuestionCount:output.candidateBankQuestionCount,issueRows:output.counts.issueRows},null,2));
