import fs from 'node:fs'; import path from 'node:path'; import vm from 'node:vm'; import { fileURLToPath } from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');
const REPORT=path.join(ROOT,'reports','hs-quadratic-svg-upgrade-20260908');
const FACTS=JSON.parse(fs.readFileSync(path.join(REPORT,'645_specialist_v1_expected_facts_r44.json')));
const V2=JSON.parse(fs.readFileSync(path.join(REPORT,'649_specialist_v2_artifact_only_r44.json')));
const VIS=JSON.parse(fs.readFileSync(path.join(REPORT,'646_specialist_candidate_visual_manifest_r44.json')));
const BANK=JSON.parse(fs.readFileSync(path.join(REPORT,'647_specialist_candidate_bank_manifest_r44.json')));
const OUTPUT=path.join(REPORT,'652_specialist_v3_parity_r44.json');
function load(relativePath){const context={window:{}};vm.createContext(context);vm.runInContext(fs.readFileSync(path.join(ROOT,relativePath),'utf8'),context,{filename:relativePath,timeout:10000});return JSON.parse(JSON.stringify(context.window));}
function format(value){if(typeof value==='number')return Number.isInteger(value)?String(value):value.toFixed(5).replace(/0+$/,'').replace(/\.$/,'');return String(value??'');}
function norm(value){return String(value??'').replace(/\\(?:d?frac)\{([^{}]+)\}\{([^{}]+)\}/g,'$1/$2').replace(/\\(?:d?frac)\s*([0-9]+)\s*([0-9]+)/g,'$1/$2').replace(/\$|\\/g,'').replace(/[{}]/g,'').replace(/[−–]/g,'-').replace(/\s+/g,'');}
function includes(haystack,needle){return norm(haystack).includes(norm(needle));}
function sourceAnchors(fact){const expected=fact.expectedFacts,anchors=[];for(const key of ['result','sum'])if(expected[key]!==undefined&&typeof expected[key]!=='object')anchors.push(format(expected[key]));return [...new Set(anchors)];}
function artifactAnchors(fact){const expected=fact.expectedFacts,anchors=[];for(const key of ['result','sum','count','maximum','minimum'])if(expected[key]!==undefined&&typeof expected[key]!=='object')anchors.push(format(expected[key]));if(expected.exact&&fact.expectedVisualType==='number-line')anchors.push(expected.exact);if(expected.solutionInterval)for(const [index,value] of expected.solutionInterval.entries())anchors.push(index===0?expected.leftLabel??format(value):expected.rightLabel??format(value));if(expected.solutionIntervals)for(const interval of expected.solutionIntervals){anchors.push(interval._leftLabel??'−∞');anchors.push(interval._rightLabel??'∞');}if(expected.integerSolutions)for(const value of expected.integerSolutions)anchors.push(format(value));if(expected.vertex)anchors.push(`(${format(expected.vertex[0])},${format(expected.vertex[1])})`);return [...new Set(anchors)];}
const v2ByUid=new Map(V2.rows.map(row=>[row.questionUid,row]));
const visualsByUid=new Map(VIS.rows.map(row=>[row.questionUid,row]));
const candidatesBySource=new Map(BANK.candidateFiles.map(file=>[file.sourcePath,file]));
const rows=[];
for(const fact of FACTS.rows){const artifact=v2ByUid.get(fact.questionUid),visual=visualsByUid.get(fact.questionUid);if(!artifact||!visual)throw new Error(`missing V2/visual ${fact.questionUid}`);const candidateFile=candidatesBySource.get(fact.sourceJsPath);if(!candidateFile)throw new Error(`missing candidate file ${fact.sourceJsPath}`);const bank=load(candidateFile.candidatePath),question=bank.questionBank.find(item=>Number(item.id)===fact.id);if(!question)throw new Error(`missing question ${fact.questionUid}`);const missingArtifact=artifactAnchors(fact).filter(anchor=>!includes(artifact.observedText,anchor));const solution=String(question.solution??'');const missingSolution=sourceAnchors(fact).filter(anchor=>!includes(solution,anchor));const hashParity=artifact.artifactFactHash===visual.factSha256;const answerPresent=typeof question.answer==='string'&&question.answer.trim().length>0;const solutionPresent=solution.trim().length>0;const verdict=hashParity&&!missingArtifact.length&&!missingSolution.length&&answerPresent&&solutionPresent?'PASS':'FAIL';rows.push({questionUid:fact.questionUid,id:fact.id,artifactHashParity:hashParity,artifactAnchorCount:artifactAnchors(fact).length,missingArtifactAnchors:missingArtifact,solutionEvidenceAnchors:sourceAnchors(fact),missingSolutionAnchors:missingSolution,sourceAnswer:question.answer,sourceAnswerPresent:answerPresent,solutionPresent,verdict,status:verdict==='PASS'?'V3_PARITY_PASS':'V3_PARITY_FAIL'});}
const failCount=rows.filter(row=>row.verdict==='FAIL').length;
const output={schemaVersion:'HS_QUADRATIC_SPECIALIST_V3_PARITY_R44',status:failCount?'V3_PARITY_FAIL':'V3_PARITY_RECORDED_NO_FINAL_PASS',productionAuthorized:false,inputVisibilityProfile:'FROZEN_V1_V2_PLUS_CANDIDATE_SOLUTION',rows,passCount:rows.length-failCount,failCount,note:'V3 row-level candidate parity only; provider-attested full-scope closure is not claimed.'};
fs.writeFileSync(OUTPUT,`${JSON.stringify(output,null,2)}\n`);console.log(JSON.stringify({status:output.status,rows:rows.length,passCount:output.passCount,failCount},null,2));
