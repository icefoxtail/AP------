import fs from 'node:fs';
import path from 'node:path';
import vm from 'node:vm';
import crypto from 'node:crypto';
import { fileURLToPath } from 'node:url';
const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..'); const REPORT = path.join(ROOT, 'reports', 'hs-quadratic-svg-upgrade-20260908'); const BASE = JSON.parse(fs.readFileSync(path.join(REPORT, '412_specialist_candidate_bank_manifest_r30.json'), 'utf8')); const FACTS = JSON.parse(fs.readFileSync(path.join(REPORT, '426_specialist_v1_expected_facts_r31.json'), 'utf8')); const VIS = JSON.parse(fs.readFileSync(path.join(REPORT, '427_specialist_candidate_visual_manifest_r31.json'), 'utf8')); const OUT = path.join(ROOT, 'archive', '_generated', 'hs-quadratic-svg-upgrade-20260908', 'candidate-r31', 'exams'); const OUTPUT = path.join(REPORT, '428_specialist_candidate_bank_manifest_r31.json');
function load(relative) { const context = { window: {} }; vm.createContext(context); vm.runInContext(fs.readFileSync(path.join(ROOT, relative), 'utf8'), context, { filename: relative, timeout: 10000 }); return JSON.parse(JSON.stringify(context.window)); }
function sha(value) { return crypto.createHash('sha256').update(value).digest('hex'); }
function key(source, id) { return `${source}|${id}`; }
function protectedHash(question) { return sha(JSON.stringify({ id: question.id ?? null, content: question.content ?? null, choices: question.choices ?? null, answer: question.answer ?? null, image: question.image ?? null })); }
const facts = new Map(FACTS.rows.map((row) => [key(row.sourceJsPath, row.id), row])); const visuals = new Map(VIS.rows.map((row) => [key(row.questionUid.split('|')[0], row.questionUid.split('|').at(-1)), row])); const rows = []; const errors = []; const candidateFiles = []; let actual = 0; fs.mkdirSync(OUT, { recursive: true });
for (let index = 0; index < BASE.candidateFiles.length; index += 1) { const file = BASE.candidateFiles[index]; const source = load(file.sourcePath); const candidate = load(file.candidatePath); for (const question of candidate.questionBank) { const visual = visuals.get(key(file.sourcePath, question.id)); if (!visual) continue; const fact = facts.get(key(file.sourcePath, question.id)); if (!fact) { errors.push(`FACT_MISSING:${file.sourcePath}|${question.id}`); continue; } const before = protectedHash(question); question.solutionImage = visual.assetPath; question.solutionImageAlt = '이차함수·이차방정식·이차부등식 핵심 조건과 해집합을 확인하는 후보 해설 시각자료'; question.solutionImageCaption = '후보 SVG: 독립 검산으로 확정한 구간·끝점·꼭짓점·핵심 값을 그림에서 확인한다.'; question.solutionImageSize = 'full'; if (before !== protectedHash(question)) errors.push(`PROTECTED_DRIFT:${file.sourcePath}|${question.id}`); rows.push({ questionUid: fact.questionUid, sourceJsPath: file.sourcePath, id: Number(question.id), caseId: visual.caseId, assetPath: visual.assetPath, protectedHashBefore: before, protectedHashAfter: protectedHash(question), status: 'SPECIALIST_CANDIDATE_VISUAL_BOUND_NO_PASS' }); } actual += candidate.questionBank.filter((question) => question.solutionImage).length; const name = `${String(index + 1).padStart(3, '0')}-${path.basename(file.sourcePath)}`; const out = path.join(OUT, name); fs.writeFileSync(out, `window.examTitle = ${JSON.stringify(source.examTitle)};\nwindow.questionBank = ${JSON.stringify(candidate.questionBank, null, 2)};\n`, 'utf8'); candidateFiles.push({ sourcePath: file.sourcePath, candidatePath: `archive/_generated/hs-quadratic-svg-upgrade-20260908/candidate-r31/exams/${name}`, sourceQuestionCount: source.questionBank.length, candidateQuestionCount: candidate.questionBank.length, candidateFileSha256: sha(fs.readFileSync(out)) }); }
if (rows.length !== FACTS.rows.length) errors.push(`ROW_COUNT:${rows.length}`); if (new Set(rows.map((row) => key(row.sourceJsPath, row.id))).size !== rows.length) errors.push('DUPLICATE_SPECIALIST_VISUAL_ROWS'); const output = { schemaVersion: 'HS_QUADRATIC_SPECIALIST_CANDIDATE_BANK_MANIFEST_R31', status: errors.length ? 'SPECIALIST_CANDIDATE_BANK_FAIL' : 'SPECIALIST_CANDIDATE_BANK_ATTACHED_NO_PASS', productionAuthorized: false, baseCandidateManifest: 'reports/hs-quadratic-svg-upgrade-20260908/412_specialist_candidate_bank_manifest_r30.json', v1Facts: 'reports/hs-quadratic-svg-upgrade-20260908/426_specialist_v1_expected_facts_r31.json', visualManifest: 'reports/hs-quadratic-svg-upgrade-20260908/427_specialist_candidate_visual_manifest_r31.json', targetQuestionCount: 430, candidateFileCount: candidateFiles.length, attachedSpecialistVisualCount: rows.length, inheritedVisualCount: BASE.totalDeclaredCandidateVisualCount, supersededInheritedVisualCount: 0, totalDeclaredCandidateVisualCount: actual, candidateFiles, rows, errors, note: 'Candidate-r31 carries r30 and adds 8 fresh specialist candidate visuals; production source/assets and final PASS remain untouched.' }; fs.writeFileSync(OUTPUT, `${JSON.stringify(output, null, 2)}\n`, 'utf8'); console.log(JSON.stringify({ status: output.status, candidateFileCount: output.candidateFileCount, attachedSpecialistVisualCount: output.attachedSpecialistVisualCount, totalDeclaredCandidateVisualCount: output.totalDeclaredCandidateVisualCount, errors: output.errors.length }, null, 2));
