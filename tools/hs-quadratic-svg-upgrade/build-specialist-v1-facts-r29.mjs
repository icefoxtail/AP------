import fs from 'node:fs';import path from 'node:path';import vm from 'node:vm';import { fileURLToPath } from 'node:url';const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const REPORT=path.join(ROOT,'reports','hs-quadratic-svg-upgrade-20260908');const BASE=JSON.parse(fs.readFileSync(path.join(REPORT,'382_specialist_candidate_bank_manifest_r28.json'),'utf8'));const OUTPUT=path.join(REPORT,'395_specialist_v1_expected_facts_r29.json');const TARGETS=[
 ['1final','22_팔마고_1학기_기말_고1_기출.js',5,'number-line',{solutionInterval:[-2,-2],leftClosed:true,rightClosed:true,result:-2,exact:'m=−2, m=3은 같은 직선'}],
 ['1final','22_팔마고_1학기_기말_고1_기출.js',9,'number-line',{solutionInterval:[-5,-5],leftClosed:true,rightClosed:true,result:-5,exact:'공통해 (2,1), a=−9,b=4, a+b=−5'}],
 ['1final','22_매산고_1학기_기말_고1_기출.js',14,'number-line',{solutionInterval:[-8,-8],leftClosed:true,rightClosed:true,result:-8,exact:'극선 3x+4y−15=0, a+b+c=−8'}],
 ['1final','22_순천여고_1학기_기말_고1_기출.js',4,'number-line',{solutionInterval:[2*Math.sqrt(10),2*Math.sqrt(10)],leftClosed:true,rightClosed:true,leftLabel:'2√10',rightLabel:'2√10',result:'2√10',exact:'k=2√10'}],
 ['1final','22_순천여고_1학기_기말_고1_기출.js',8,'number-line',{solutionInterval:[2,2],leftClosed:true,rightClosed:true,result:2,exact:'a=−1,r=3, a+r=2'}],
 ['1final','22_순천여고_1학기_기말_고1_기출.js',9,'number-line',{solutionInterval:[3,3],leftClosed:true,rightClosed:true,result:3,exact:'평행사변형 C=(5−a,1), G=(2,1), p+q=3'}],
 ['1final','22_순천여고_1학기_기말_고1_기출.js',10,'number-line',{solutionInterval:[-1,-1],leftClosed:true,rightClosed:true,result:-1,exact:'BD:DC=3:1, D=(3,4), a−b=−1'}],
 ['1final','22_순천여고_1학기_기말_고1_기출.js',12,'number-line',{solutionInterval:[9,9],leftClosed:true,rightClosed:true,result:'9π',exact:'중심 (2,3), r=3, 넓이=9π'}],
];function load(relative){const c={window:{}};vm.createContext(c);vm.runInContext(fs.readFileSync(path.join(ROOT,relative),'utf8'),c,{filename:relative,timeout:10000});return JSON.parse(JSON.stringify(c.window));}const baseBySource=new Map(BASE.candidateFiles.map(f=>[f.sourcePath,f])),rows=[];for(const [dir,basename,id,expectedVisualType,expectedFacts] of TARGETS){const sourceJsPath=`archive/exams/original/high/h1/${dir}/${basename}`;const source=load(sourceJsPath);const question=source.questionBank.find(item=>Number(item.id)===id);if(!question)throw new Error(`missing ${sourceJsPath} q${id}`);const base=load(baseBySource.get(sourceJsPath).candidatePath);const candidateQuestion=base.questionBank.find(item=>Number(item.id)===id);if(candidateQuestion?.solutionImage)throw new Error(`already candidate visualized ${sourceJsPath} q${id}`);rows.push({questionUid:`${sourceJsPath}|${source.examTitle}|${id}`,sourceJsPath,id,content:question.content,choices:question.choices??[],expectedVisualType,expectedFacts});}const output={schemaVersion:'HS_QUADRATIC_SPECIALIST_V1_EXPECTED_FACTS_R29',status:'EXPECTED_FACTS_FROZEN_SOURCE_ONLY_CANDIDATE_NO_PASS',inputVisibilityProfile:'SOURCE_ONLY',priorReviewVisibility:'NONE',rows,note:'Fresh source-only facts for 8 specialist rows; existing solution/SVG/V2/V3 evidence were not used for derivation.'};fs.writeFileSync(OUTPUT,`${JSON.stringify(output,null,2)}\n`,'utf8');console.log(JSON.stringify({status:output.status,rows:rows.length,numberLineRows:rows.filter(r=>r.expectedVisualType==='number-line').length,cartesianRows:rows.filter(r=>r.expectedVisualType==='cartesian').length},null,2));
