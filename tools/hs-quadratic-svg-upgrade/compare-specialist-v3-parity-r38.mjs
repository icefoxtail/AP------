import fs from 'node:fs';
import path from 'node:path';
import vm from 'node:vm';
import { fileURLToPath } from 'node:url';
const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..'); const REPORT = path.join(ROOT, 'reports', 'hs-quadratic-svg-upgrade-20260908'); const FACTS = JSON.parse(fs.readFileSync(path.join(REPORT, '541_specialist_v1_expected_facts_r38.json'), 'utf8')); const V2 = JSON.parse(fs.readFileSync(path.join(REPORT, '545_specialist_v2_artifact_only_r38.json'), 'utf8')); const VIS = JSON.parse(fs.readFileSync(path.join(REPORT, '542_specialist_candidate_visual_manifest_r38.json'), 'utf8')); const BANK = JSON.parse(fs.readFileSync(path.join(REPORT, '543_specialist_candidate_bank_manifest_r38.json'), 'utf8')); const OUTPUT = path.join(REPORT, '548_specialist_v3_parity_r38.json');
function load(relative) { const context = { window: {} }; vm.createContext(context); vm.runInContext(fs.readFileSync(path.join(ROOT, relative), 'utf8'), context, { filename: relative, timeout: 10000 }); return JSON.parse(JSON.stringify(context.window)); }
function format(value) { if (typeof value === 'number') return Number.isInteger(value) ? String(value) : value.toFixed(5).replace(/0+$/, '').replace(/\.$/, ''); return String(value ?? ''); }
function norm(value) { return String(value ?? '').replace(/\\(?:d?frac)\{([^{}]+)\}\{([^{}]+)\}/g, '$1/$2').replace(/\$|\\/g, '').replace(/[{}]/g, '').replace(/[−–]/g, '-').replace(/\s+/g, ''); }
function includes(haystack, needle) { return norm(haystack).includes(norm(needle)); }
function sourceAnchors(fact) { const anchors = []; if (typeof fact.expectedFacts.result === 'number') anchors.push(format(fact.expectedFacts.result)); if (typeof fact.expectedFacts.sum === 'number') anchors.push(format(fact.expectedFacts.sum)); return [...new Set(anchors)]; }
function artifactAnchors(fact) { const e = fact.expectedFacts; const anchors = []; for (const key of ['result', 'sum', 'count', 'maximum', 'minimum']) if (e[key] !== undefined && typeof e[key] !== 'object') anchors.push(format(e[key])); if (e.exact && fact.expectedVisualType === 'number-line') anchors.push(e.exact); if (e.solutionInterval) for (const [index, value] of e.solutionInterval.entries()) anchors.push(index === 0 ? e.leftLabel ?? format(value) : e.rightLabel ?? format(value)); if (e.vertex) anchors.push(`(${format(e.vertex[0])},${format(e.vertex[1])})`); return [...new Set(anchors)]; }
const v2 = new Map(V2.rows.map((row) => [row.questionUid, row])); const visuals = new Map(VIS.rows.map((row) => [row.questionUid, row])); const candidates = new Map(BANK.candidateFiles.map((file) => [file.sourcePath, file])); const rows = [];
for (const fact of FACTS.rows) { const artifact = v2.get(fact.questionUid); const visual = visuals.get(fact.questionUid); if (!artifact || !visual) throw new Error(`missing V2/visual ${fact.questionUid}`); const bank = load(candidates.get(fact.sourceJsPath).candidatePath); const question = bank.questionBank.find((item) => Number(item.id) === fact.id); const missingArtifact = artifactAnchors(fact).filter((anchor) => !includes(artifact.observedText, anchor)); const solution = String(question.solution ?? ''); const missingSolution = sourceAnchors(fact).filter((anchor) => !includes(solution, anchor)); const hash = artifact.artifactFactHash === visual.factSha256; const answer = typeof question.answer === 'string' && question.answer.trim().length > 0; const present = solution.trim().length > 0; const verdict = hash && !missingArtifact.length && !missingSolution.length && answer && present ? 'PASS' : 'FAIL'; rows.push({ questionUid: fact.questionUid, id: fact.id, artifactHashParity: hash, artifactAnchorCount: artifactAnchors(fact).length, missingArtifactAnchors: missingArtifact, solutionEvidenceAnchors: sourceAnchors(fact), missingSolutionAnchors: missingSolution, sourceAnswer: question.answer, sourceAnswerPresent: answer, solutionPresent: present, verdict, status: verdict === 'PASS' ? 'V3_PARITY_PASS' : 'V3_PARITY_FAIL' }); }
const failCount = rows.filter((row) => row.verdict === 'FAIL').length; const output = { schemaVersion: 'HS_QUADRATIC_SPECIALIST_V3_PARITY_R38', status: failCount ? 'V3_PARITY_FAIL' : 'V3_PARITY_RECORDED_NO_FINAL_PASS', productionAuthorized: false, inputVisibilityProfile: 'FROZEN_V1_V2_PLUS_CANDIDATE_SOLUTION', rows, passCount: rows.length - failCount, failCount, note: 'V3 row-level candidate parity only; provider-attested full-scope closure is not claimed.' };
fs.writeFileSync(OUTPUT, `${JSON.stringify(output, null, 2)}\n`, 'utf8'); console.log(JSON.stringify({ status: output.status, rows: rows.length, passCount: output.passCount, failCount }, null, 2));
