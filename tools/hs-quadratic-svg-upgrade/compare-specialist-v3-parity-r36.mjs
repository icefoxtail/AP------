import fs from 'node:fs';
import path from 'node:path';
import vm from 'node:vm';
import { fileURLToPath } from 'node:url';
const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..'); const REPORT = path.join(ROOT, 'reports', 'hs-quadratic-svg-upgrade-20260908'); const FACTS = JSON.parse(fs.readFileSync(path.join(REPORT, '506_specialist_v1_expected_facts_r36.json'), 'utf8')); const V2 = JSON.parse(fs.readFileSync(path.join(REPORT, '510_specialist_v2_artifact_only_r36.json'), 'utf8')); const VIS = JSON.parse(fs.readFileSync(path.join(REPORT, '507_specialist_candidate_visual_manifest_r36.json'), 'utf8')); const BANK = JSON.parse(fs.readFileSync(path.join(REPORT, '508_specialist_candidate_bank_manifest_r36.json'), 'utf8')); const OUTPUT = path.join(REPORT, '513_specialist_v3_parity_r36.json'); function load(relative) { const context = { window: {} }; vm.createContext(context); vm.runInContext(fs.readFileSync(path.join(ROOT, relative), 'utf8'), context, { filename: relative, timeout: 10000 }); return JSON.parse(JSON.stringify(context.window)); } function format(value) { if (typeof value === 'number') return Number.isInteger(value) ? String(value) : value.toFixed(5).replace(/0+$/, '').replace(/\.$/, ''); return String(value ?? ''); } function rational(value) { if (Number.isInteger(value)) return null; for (let denominator = 2; denominator <= 12; denominator += 1) { const numerator = Math.round(value * denominator); if (Math.abs(value - numerator / denominator) < 1e-8) { const gcd = (a, b) => b === 0 ? a : gcd(b, a % b); const g = gcd(Math.abs(numerator), denominator) || 1; return `${numerator / g}/${denominator / g}`; } } return null; } function norm(value) { return String(value ?? '').replace(/\\(?:d?frac)\{([^{}]+)\}\{([^{}]+)\}/g, '$1/$2').replace(/\$|\\/g, '').replace(/[{}]/g, '').replace(/[−–]/g, '-').replace(/\s+/g, ''); } function includes(haystack, needle) { return norm(haystack).includes(norm(needle)); } function sourceAnchors(fact) { const anchors = []; for (const key of ['sum', 'result']) if (typeof fact.expectedFacts[key] === 'number') anchors.push(rational(fact.expectedFacts[key]) ?? format(fact.expectedFacts[key])); return [...new Set(anchors)]; } function artifactAnchors(fact) { const expected = fact.expectedFacts; const anchors = []; for (const key of ['result', 'sum', 'count', 'maximum', 'minimum']) if (expected[key] !== undefined && typeof expected[key] !== 'object') anchors.push(format(expected[key])); if (expected.exact && fact.expectedVisualType === 'number-line') anchors.push(expected.exact); for (const key of ['solutionInterval', 'realInterval']) if (expected[key]) for (const [index, value] of expected[key].entries()) if (value !== null) anchors.push(index === 0 ? expected.leftLabel ?? format(value) : expected.rightLabel ?? format(value)); if (expected.integerSolutions) for (const value of expected.integerSolutions) anchors.push(format(value)); if (expected.vertex) anchors.push(`(${format(expected.vertex[0])},${format(expected.vertex[1])})`); return [...new Set(anchors)]; } const v2 = new Map(V2.rows.map((row) => [row.questionUid, row])); const visuals = new Map(VIS.rows.map((row) => [row.questionUid, row])); const candidates = new Map(BANK.candidateFiles.map((file) => [file.sourcePath, file])); const rows = []; for (const fact of FACTS.rows) { const artifact = v2.get(fact.questionUid); const visual = visuals.get(fact.questionUid); if (!artifact || !visual) throw new Error(`missing V2/visual ${fact.questionUid}`); const bank = load(candidates.get(fact.sourceJsPath).candidatePath); const question = bank.questionBank.find((item) => Number(item.id) === fact.id); const missingArtifact = artifactAnchors(fact).filter((anchor) => !includes(artifact.observedText, anchor)); const solution = String(question.solution ?? ''); const missingSolution = sourceAnchors(fact).filter((anchor) => !includes(solution, anchor)); const hash = artifact.artifactFactHash === visual.factSha256; const answer = typeof question.answer === 'string' && question.answer.trim().length > 0; const present = solution.trim().length > 0; const verdict = hash && !missingArtifact.length && !missingSolution.length && answer && present ? 'PASS' : 'FAIL'; rows.push({ questionUid: fact.questionUid, id: fact.id, artifactHashParity: hash, artifactAnchorCount: artifactAnchors(fact).length, missingArtifactAnchors: missingArtifact, solutionEvidenceAnchors: sourceAnchors(fact), missingSolutionAnchors: missingSolution, sourceAnswer: question.answer, sourceAnswerPresent: answer, solutionPresent: present, verdict, status: verdict === 'PASS' ? 'V3_PARITY_PASS' : 'V3_PARITY_FAIL' }); } const failCount = rows.filter((row) => row.verdict === 'FAIL').length; const output = { schemaVersion: 'HS_QUADRATIC_SPECIALIST_V3_PARITY_R36', status: failCount ? 'V3_PARITY_FAIL' : 'V3_PARITY_RECORDED_NO_FINAL_PASS', productionAuthorized: false, inputVisibilityProfile: 'FROZEN_V1_V2_PLUS_CANDIDATE_SOLUTION', rows, passCount: rows.length - failCount, failCount, note: 'V3 row-level candidate parity only; provider-attested full-scope closure is not claimed.' }; fs.writeFileSync(OUTPUT, `${JSON.stringify(output, null, 2)}\n`, 'utf8'); console.log(JSON.stringify({ status: output.status, rows: rows.length, passCount: output.passCount, failCount }, null, 2));
