import fs from 'node:fs';
import path from 'node:path';
import vm from 'node:vm';
import crypto from 'node:crypto';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..');
const REPORT = path.join(ROOT, 'reports', 'hs-quadratic-svg-upgrade-20260908');
const INVENTORY = path.join(REPORT, '01_target_inventory.csv');
const CLEAN = JSON.parse(fs.readFileSync(path.join(REPORT, '59_clean_candidate_bank_manifest_r10.json'), 'utf8'));
const OUT = path.join(ROOT, 'archive', '_generated', 'hs-quadratic-svg-upgrade-20260908', 'candidate-r10-scoped', 'exams');
const OUTPUT = path.join(REPORT, '62_scoped_candidate_bank_manifest_r10.json');
const TARGET_KEYS = new Set(['H15-SA-05', 'H15-SA-08', 'H15-SA-13', 'H22-C-05', 'H22-C-06']);
function sha(value) { return crypto.createHash('sha256').update(value).digest('hex'); }
function parseCsv(text) { const rows = []; let row = []; let cell = ''; let quoted = false; for (let i = 0; i < text.length; i += 1) { const ch = text[i]; const next = text[i + 1]; if (quoted && ch === '"' && next === '"') { cell += '"'; i += 1; continue; } if (ch === '"') { quoted = !quoted; continue; } if (!quoted && ch === ',') { row.push(cell); cell = ''; continue; } if (!quoted && ch === '\n') { row.push(cell.replace(/\r$/, '')); rows.push(row); row = []; cell = ''; continue; } cell += ch; } if (cell || row.length) { row.push(cell); rows.push(row); } const headers = rows.shift(); return rows.map(values => Object.fromEntries(headers.map((key, i) => [key, values[i] ?? '']))); }
function load(relative) { const context = { window: {} }; vm.createContext(context); vm.runInContext(fs.readFileSync(path.join(ROOT, relative), 'utf8'), context, { filename: relative, timeout: 10000 }); return JSON.parse(JSON.stringify(context.window)); }
const inventory = parseCsv(fs.readFileSync(INVENTORY, 'utf8')).filter(row => TARGET_KEYS.has(row.standardUnitKey)); const idsBySource = new Map(); for (const row of inventory) { const sourcePath = row.sourceJsPath.startsWith('archive/') ? row.sourceJsPath : `archive/${row.sourceJsPath}`; if (!idsBySource.has(sourcePath)) idsBySource.set(sourcePath, []); idsBySource.get(sourcePath).push(Number(row.id)); }
const cleanBySource = new Map(CLEAN.candidateFiles.map(file => [file.sourcePath, file])); const candidateFiles = []; let targetRows = 0;
for (const [sourcePath, ids] of idsBySource) { const cleanFile = cleanBySource.get(sourcePath); if (!cleanFile) throw new Error(`clean candidate missing ${sourcePath}`); const bank = load(cleanFile.candidatePath); const selected = bank.questionBank.filter(q => ids.includes(Number(q.id))); if (selected.length !== ids.length) throw new Error(`scoped id mismatch ${sourcePath}`); const outputName = `${String(candidateFiles.length + 1).padStart(3, '0')}-${path.basename(sourcePath)}`; const outputPath = path.join(OUT, outputName); fs.mkdirSync(OUT, { recursive: true }); fs.writeFileSync(outputPath, `window.examTitle = ${JSON.stringify(bank.examTitle)};\nwindow.questionBank = ${JSON.stringify(selected, null, 2)};\n`, 'utf8'); candidateFiles.push({ sourcePath, candidatePath: `archive/_generated/hs-quadratic-svg-upgrade-20260908/candidate-r10-scoped/exams/${outputName}`, targetQuestionCount: selected.length, candidateFileSha256: sha(fs.readFileSync(outputPath)) }); targetRows += selected.length; }
const output = { schemaVersion: 'HS_QUADRATIC_SCOPED_CANDIDATE_BANK_MANIFEST_R10', status: 'SCOPED_CANDIDATE_BANK_BUILT_NO_PASS', productionAuthorized: false, inventoryPath: 'reports/hs-quadratic-svg-upgrade-20260908/01_target_inventory.csv', targetQuestionCount: targetRows, sourceFileCount: candidateFiles.length, candidateQuestionCount: targetRows, candidateFiles, inheritedCleanCandidateManifest: 'reports/hs-quadratic-svg-upgrade-20260908/59_clean_candidate_bank_manifest_r10.json', note: 'Scoped candidate files contain exactly the 430 locked target questions, preserving current source repair and explicitly reviewed candidate solution/visual fields. They are pipeline inputs only and do not establish V1/V2/V3 or final PASS.' };
fs.writeFileSync(OUTPUT, `${JSON.stringify(output, null, 2)}\n`, 'utf8'); console.log(JSON.stringify({ status: output.status, targetQuestionCount: output.targetQuestionCount, sourceFileCount: output.sourceFileCount, candidateQuestionCount: output.candidateQuestionCount }, null, 2));
