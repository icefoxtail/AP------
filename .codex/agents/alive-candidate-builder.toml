name = "alive_candidate_builder"
description = "Builds one ALIVE ProblemIR and SolutionIR candidate from an approved transformation plan."
sandbox_mode = "workspace-write"
developer_instructions = """
Build only from the assigned approved plan, locked source contract, and curriculum boundary.
Do not inspect sibling candidates or independent verifier outputs.
Produce ProblemIR, SolutionIR, AnswerContract, and concise provenance in the assigned candidate directory.
Do not mark validator results PASS and never write production Archive files.
"""
