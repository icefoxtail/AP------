# 2024년 고1 1학기 기말 `원의 방정식` 작업 검수 기록

## 대상 확정

2024년 고1 1학기 기말 원본 JS 3개를 DB·question-index·production JS와 대조하여 `H15-SA-11 / 원의 방정식`만 선별했다.

| 시험지 | 전체 문항 | 대상 문항 |
|---|---:|---|
| 24 금당고 1학기 기말 | 20 | q7, q9, q16, q20 |
| 24 매산고 1학기 기말 | 20 | q7, q9, q15, q17 |
| 24 제일고 1학기 기말 | 22 | q10, q12, q13, q15, q17, q21, q22 |

총 15문항이다. 2024년 고1 1학기 기말 동일 범위에서 추가 대상 시험지는 확인되지 않았다.

## 작업 결과

- 기존 해설 14문항: 기존 풀이를 유지하고 학생의 재현을 돕는 `[보강]`과 도형을 추가했다.
- 제일고 q22: 기존 해설이 문항의 원과 불일치하여 현재 문항 기준으로만 해설을 재작성했다. 문제 본문·선지·정답·메타데이터는 유지했다.
- 대상 SVG 15개를 생성하고 JS의 `solutionImage`, Alt, Caption, Size를 연결했다.
- 최초 실제 렌더에서 확인된 금당고 q7 라벨 겹침과 제일고 q10 도형 여백 문제는 해당 SVG만 핀포인트 수정했다.

## 정적 대조

- 3개 JS `node --check`: PASS
- SVG 15개 XML 파싱: PASS
- question-index 재생성: 10,868문항 / 중복 0
- 3개 archive audit: PASS, 각 시험지의 JS 문항 수와 index 수 일치
- 대상 밖·보호 문항 변경: 0건
- 대상별 변경 필드: `solution`, `solutionImage`, `solutionImageAlt`, `solutionImageCaption`, `solutionImageSize`만 변경
- 대상의 문제 본문·선지·정답·메타데이터: 보존

## 실제 production 렌더

### 해설지 `sol`

- 금당고: q-box 23개(연속 해설 박스 포함), 고유 문항 1–20, 대상 SVG 4개 로드, 대상 overflow·load error 없음
- 매산고: q-box 24개(연속 해설 박스 포함), 고유 문항 1–20, 대상 SVG 4개 로드, 대상 overflow·load error 없음
- 제일고: q-box 26개(연속 해설 박스 포함), 고유 문항 1–22, 대상 SVG 7개 로드, 대상 overflow·load error 없음
- 15개 대상 도형을 화면에서 직접 확인했으며, 중심·반지름·직선·교점·라벨이 해설 내용과 일치하고 잘림·겹침이 없도록 확인했다.

### 문제지 `exam` / 답지 `ans`

- 금당고: `exam` 20문항, `ans` 20답
- 매산고: `exam` 20문항, `ans` 20답
- 제일고: `exam` 22문항, `ans` 22답
- 세 시험지 모두 이미지 로드 오류와 전체 폭 초과를 확인하지 못했다.

## 판정

- 2024년 1학기 기말 대상 15문항의 자체 작업·범위 대조·production 렌더: **PASS**
- 독립검수 1차에서 15문항은 모두 **PASS**였다. 2차에서 제일고 q12의 F₂ 원 하단 클리핑 1건이 **FAIL**로 확인되었다.
- q12 SVG만 핀포인트 수정했다. 주황색 F₂ 원은 `cy=226, r=88`에서 `cy=208, r=88`로 조정되어 하단이 `314`에서 `296`이 되었고, `viewBox` 하단 300 안에 들어온다.
- 수정 후 q12 실제 `sol` 화면에서 SVG 640×300 로드, 도형 전체 표시, 대상 overflow·load error 없음으로 재확인했다.
- 수정 후 제일고 전체 `exam` 22문항·`ans` 22답, 이미지 로드 오류·전체 폭 초과·렌더 오류 없음으로 재확인했다. 3개 시험지 archive audit도 PASS다.
- 현재 대상 범위는 **15문항 PASS(초기 독립 FAIL 1건 수정 후 해소)** 상태다. 수정 후 독립검수 재실행 결과가 도착하면 최종 출고 판정을 봉인한다.
- 보호 문항에서 발견되는 기존 문제는 이 기록에 남기되, 사용자 승인 전에는 수정하지 않는다.
