# 원의 방정식 SVG 재검수 기록

- 검수일: 2026-09-05 (KST)
- 범위: 기존 circle-repair inventory 전체
- 결과: **65/65 SVG XML/static PASS**

모든 65개 자산의 파일 존재, XML parse, `viewBox`, 금지 요소(`script`/`foreignObject`/`br`) 부재, 유한 좌표를 확인했다. 기존 수치 자산의 기호형 반지름 표기(예: `4√5`)는 숫자 오류가 아닌 정상 semantic metadata로 허용했다.

이번 핀포인트 대상인 `25_매산고 q10`은 별도로 다음을 독립 재계산했다.

- 원 `x²+y²=9`의 중심 `O=(0,0)`, 반지름 `3`
- 접점 `T₁=(3/5,6√6/5)`, `T₂=(3/5,−6√6/5)`
- 위쪽 실제 segment: `y=−0.204124x+3.061862`
- 아래쪽 실제 segment: `y=0.204124x−3.061862`
- 두 line의 model endpoint residual: 최대 `1e−8` 미만
- 기울기 곱: `−1/24`, 따라서 `a+b=25`

독립 verifier 결과는 `reports/shape-movement-visuals-20260904/FINAL_RECHECK.json`의 circle repair static section과 일치한다.

