{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build archive DB",
            "type": "process",
            "command": "${workspaceFolder}\\build_archive_db.cmd",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
