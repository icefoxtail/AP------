interface:
  display_name: "APMath ALIVE Unified Pipeline"
  short_description: "4배치 Luna xhigh 전체 생성·검수·렌더·패키지"
  default_prompt: "Use $apmath-similar-question-pipeline to generate a whole similar exam through the unified FOUR_BALANCED Luna xhigh route: lock source and rules, inspect source visuals, generate all batches, independently review, revise bounded findings, run full second review and mother final, render exam/solution/answer in the production browser, and package locally without Archive publication."
policy:
  allow_implicit_invocation: true
