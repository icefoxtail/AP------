interface:
  display_name: "APMath ALIVE 4-Batch (Legacy Alias)"
  short_description: "통합 4배치 파이프라인으로 연결하는 호환용 별칭"
  default_prompt: "Use $apmath-similar-question-pipeline instead; this name is retained only as a compatibility alias for the unified FOUR_BALANCED Luna xhigh workflow."
policy:
  allow_implicit_invocation: false
