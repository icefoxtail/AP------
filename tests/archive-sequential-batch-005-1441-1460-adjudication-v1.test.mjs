import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {adjudicateSequentialBatch00514411460V1} from '../archive/tools/intelligence/adjudicate-sequential-batch-005-1441-1460-v1.mjs';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const outputPath=path.join(root,'archive','_generated','intelligence','phase3','sequential-review','archive-sequential-batch-005-1441-1460-adjudication-v1.json');const report=adjudicateSequentialBatch00514411460V1();const saved=JSON.parse(fs.readFileSync(outputPath,'utf8'));assert.equal(report.digest,saved.digest);assert.equal(report.totals.records,20);assert.equal(report.totals.answerRecheckConfirmed,18);assert.equal(report.totals.wordingReviewRequired,2);assert.equal(report.totals.status.DRAFT_TAXONOMY_HOLD,18);assert.equal(report.totals.status.EVIDENCE_MISSING_HOLD,2);assert.deepEqual(report.records.map(r=>r.sequenceOrder),Array.from({length:20},(_,i)=>1441+i));console.log(JSON.stringify({ok:true,digest:report.digest,totals:report.totals},null,2));
