import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {adjudicateSequentialBatch00617611780V1} from '../archive/tools/intelligence/adjudicate-sequential-batch-006-1761-1780-v1.mjs';const here=path.dirname(fileURLToPath(import.meta.url));const outputPath=path.join(here,'..','archive','_generated','intelligence','phase3','sequential-review','archive-sequential-batch-006-1761-1780-adjudication-v1.json');const saved=JSON.parse(fs.readFileSync(outputPath,'utf8'));const report=adjudicateSequentialBatch00617611780V1();if(report.digest!==saved.digest)throw new Error(`digest mismatch ${report.digest} !== ${saved.digest}`);if(report.totals.records!==20||report.totals.answerRecheckConfirmed!==20||report.totals.wordingReviewRequired!==0)throw new Error(`unexpected totals ${JSON.stringify(report.totals)}`);if(report.records[0].sequenceOrder!==1761||report.records.at(-1).sequenceOrder!==1780)throw new Error('sequence range mismatch');console.log(JSON.stringify({ok:true,digest:report.digest,totals:report.totals},null,2));
