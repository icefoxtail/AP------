# Verified Decisions

- Do not install Hermes Agent into this project for the textbook pipeline.
- Do not add Hermes as a dependency.
- Do not copy Hermes external code into this repository.
- A report-only operating layer is useful for `archive/textbook`.
- `.agent` is a helper layer, not a replacement for the upper project rulebook.
- `generated/js` is protected by default.
- Answer entry must prefer source evidence and must not rely on display number alone.
- Automatic `solution` generation is excluded by default.
- Legacy maps without `sourceDisplayNo` evidence should not be treated as accepted source identity.
- `git add`, `git commit`, and `git push` are forbidden unless explicitly requested.

