import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..','..');const OUT=path.join(ROOT,'archive','tools','logic-visual-audit','reports');const v=JSON.parse(fs.readFileSync(path.join(OUT,'phase2_v1_independent_batch_04.json'),'utf8'));const sha=(x)=>`sha256:${crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex')}`;const entries=v.entries.map(e=>({questionUid:e.questionUid,independentSignal:e.expectedSignal,independentType:e.visualType,finalVisualRequirement:'VISUAL_REQUIRED',finalVisualType:e.visualType,visualAction:e.questionUid.endsWith('|8')||e.questionUid.endsWith('|20')?'KEEP_EXISTING':'REBUILD_EXISTING',status:'RESOLVED',reason:e.rationale}));const out={generatedAtKst:'2026-09-05',phase:'LOGIC_VISUAL_PHASE_2_BATCH_04_REQUIREMENT_ADJUDICATION',status:'RESOLVED',reviewerRole:'DESIGNATED_SEMANTIC_ADJUDICATOR',priorReviewVisibility:'V1_FIRST_PASS_AND_PILOT_RULES',pendingCount:0,entries,reportSha:sha(entries)};fs.writeFileSync(path.join(OUT,'phase2_batch_04_requirement_adjudication.json'),JSON.stringify(out,null,2)+'\n','utf8');console.log(JSON.stringify({status:out.status,resolvedCount:entries.length,pendingCount:0,reportSha:out.reportSha},null,2));
