import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { sha256 } from './lib/canonicalize.mjs';
const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..', '..');
const OUT = path.join(ROOT, 'archive/tools/logic-visual-audit/reports');
const manifest = JSON.parse(fs.readFileSync(path.join(OUT, 'v1_source_only_manifest.json'), 'utf8'));
const preflight = JSON.parse(fs.readFileSync(path.join(OUT, 'rule_preflight.json'), 'utf8'));
const evidence = { freezeVersion: 'V1_FIRST_PASS_EVIDENCE_v1', createdAtKst: '2026-09-05', reviewerRole: 'V1_EXPECTED', reviewerId: 'QUALIFICATION_INFRASTRUCTURE_SEED', priorReviewVisibility: 'SOURCE_ONLY_BUNDLE_NO_ANSWER_NO_SOLUTION', inputBundleSha: manifest.manifestSha, appliedRuleRefs: preflight.rules, coverageCount: manifest.coverageCount, targetCount: manifest.targetCount, coverage: manifest.coverage, status: 'FROZEN_SOURCE_ONLY_BUNDLE_READY_FOR_INDEPENDENT_REVIEW', firstPassEvidenceSha: sha256({ manifestSha: manifest.manifestSha, ruleRoutingBundleSha: preflight.ruleRoutingBundleSha, reviewerRole: 'V1_EXPECTED', coverageCount: manifest.coverageCount }) };
fs.writeFileSync(path.join(OUT, 'v1_evidence_freeze.json'), JSON.stringify(evidence, null, 2) + '\n', 'utf8');
console.log(JSON.stringify({ status: evidence.status, coverage: evidence.coverage, firstPassEvidenceSha: evidence.firstPassEvidenceSha }, null, 2));
