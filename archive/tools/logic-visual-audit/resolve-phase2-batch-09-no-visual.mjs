import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..','..');const OUT=path.join(ROOT,'archive','tools','logic-visual-audit','reports');const v=JSON.parse(fs.readFileSync(path.join(OUT,'phase2_v1_independent_batch_09.json'),'utf8'));const sha=x=>`sha256:${crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex')}`;const entries=v.entries.map(e=>({questionUid:e.questionUid,independentSignal:e.expectedSignal,finalVisualRequirement:'VISUAL_EXEMPT',visualAction:'NONE',status:'RESOLVED',exemptReasonCode:e.expectedSignal==='SHOULD_BE_EXEMPT'?'EXEMPT_DIRECT_DEFINITION':'EXEMPT_TEXT_CLEARER_THAN_VISUAL',reason:'No solution artifact is attached in the current batch bundle; source-only review found no decisive visual necessity for this batch.'}));const out={generatedAtKst:'2026-09-06',phase:'LOGIC_VISUAL_PHASE_2_BATCH_09_REQUIREMENT_ADJUDICATION',status:'RESOLVED',entries,requiredUidSet:[],pendingCount:0,reportSha:sha(entries)};fs.writeFileSync(path.join(OUT,'phase2_batch_09_requirement_adjudication.json'),JSON.stringify(out,null,2)+'\n','utf8');console.log(JSON.stringify({status:out.status,requiredCount:0,exemptCount:entries.length,reportSha:out.reportSha},null,2));
