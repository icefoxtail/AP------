import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..','..');const OUT=path.join(ROOT,'archive','tools','logic-visual-audit','reports');const v1=JSON.parse(fs.readFileSync(path.join(OUT,'phase2_v1_independent_batch_06.json'),'utf8'));const sha=v=>`sha256:${crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex')}`;const entries=v1.entries.map(e=>({questionUid:e.questionUid,independentSignal:e.expectedSignal,independentType:e.visualType,finalVisualRequirement:'VISUAL_EXEMPT',visualAction:'NONE',exemptReasonCode:e.expectedSignal==='SHOULD_BE_EXEMPT'?'EXEMPT_DIRECT_DEFINITION':'EXEMPT_TEXT_CLEARER_THAN_VISUAL',status:'RESOLVED',reason:'No attached solution visual is present in the batch-06 artifact bundle; V1 source-only triage judged the optional visual non-decisive, so no visual is added by this qualification loop.'}));const out={generatedAtKst:'2026-09-06',phase:'LOGIC_VISUAL_PHASE_2_BATCH_06_REQUIREMENT_ADJUDICATION',status:'RESOLVED',reviewerRole:'DESIGNATED_SEMANTIC_ADJUDICATOR',pendingCount:0,entries,requiredUidSet:[],reportSha:sha(entries)};fs.writeFileSync(path.join(OUT,'phase2_batch_06_requirement_adjudication.json'),JSON.stringify(out,null,2)+'\n','utf8');console.log(JSON.stringify({status:out.status,resolvedCount:entries.length,requiredCount:0,reportSha:out.reportSha},null,2));
