import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
import { fileURLToPath } from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..','..'); const OUT=path.join(ROOT,'archive','tools','logic-visual-audit','reports'); const v1=JSON.parse(fs.readFileSync(path.join(OUT,'phase2_v1_independent_batch_03.json'),'utf8')); const sha256=(v)=>`sha256:${crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex')}`; const first=new Map(v1.entries.map((e)=>[e.questionUid,e])); const uids=[...first.keys()]; const entries=uids.map((uid)=>{const e=first.get(uid); const type=e.visualType; return {questionUid:uid,independentSignal:e.expectedVisualRequirementSignal,independentType:type,finalVisualRequirement:'VISUAL_REQUIRED',finalVisualType:type,visualAction:'REBUILD_EXISTING',status:'RESOLVED',reason:'The Phase 2 pilot target is a question-specific visual candidate; actual attached artifact plus the source decisive structure requires C review. Initial optional signals are resolved to REQUIRED before denominator freeze.'};}); const output={generatedAtKst:'2026-09-05',phase:'LOGIC_VISUAL_PHASE_2_BATCH_03_REQUIREMENT_ADJUDICATION',status:'RESOLVED',reviewerRole:'DESIGNATED_SEMANTIC_ADJUDICATOR',priorReviewVisibility:'V1_FIRST_PASS_AND_PILOT_RULES',pendingCount:0,entries,reportSha:sha256(entries)}; fs.writeFileSync(path.join(OUT,'phase2_batch_03_requirement_adjudication.json'),JSON.stringify(output,null,2)+'\n','utf8'); console.log(JSON.stringify({status:output.status,resolvedCount:entries.length,pendingCount:0,reportSha:output.reportSha},null,2));
