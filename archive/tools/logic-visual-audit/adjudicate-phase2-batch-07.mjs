import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..','..');const OUT=path.join(ROOT,'archive','tools','logic-visual-audit','reports');const v1=JSON.parse(fs.readFileSync(path.join(OUT,'phase2_v1_independent_batch_07.json'),'utf8'));const sha=v=>`sha256:${crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex')}`;const entries=v1.entries.map(e=>{const required=e.expectedSignal==='SHOULD_BE_REQUIRED';return{questionUid:e.questionUid,expectedSignal:e.expectedSignal,expectedVisualType:e.canonicalVisualType,finalVisualRequirement:required?'VISUAL_REQUIRED':'VISUAL_EXEMPT',visualAction:required?'ADD':'NONE',status:required?'BLOCKED_REQUIRED_VISUAL_MISSING':'RESOLVED_EXEMPT_NO_ARTIFACT',reason:required?'V1 requires a decisive visual but V2 bundle has no artifact; build a question-specific SVG before denominator freeze.':'No attached artifact exists and source-only review says text is sufficient; no visual is added.',nextAction:required?'BUILD_ADD_NEW_VISUAL_AND_RERUN_V2':'NO_VISUAL_C_ITEM_NOT_REQUIRED'};});const out={generatedAtKst:'2026-09-06',phase:'LOGIC_VISUAL_PHASE_2_BATCH_07_ADJUDICATION',status:entries.some(e=>e.status==='BLOCKED_REQUIRED_VISUAL_MISSING')?'REQUIRED_VISUAL_SATISFIER_MISSING':'RESOLVED',entries,blockedCount:entries.filter(e=>e.status==='BLOCKED_REQUIRED_VISUAL_MISSING').length,reportSha:sha(entries)};fs.writeFileSync(path.join(OUT,'phase2_batch_07_adjudication.json'),JSON.stringify(out,null,2)+'\n','utf8');console.log(JSON.stringify({status:out.status,blockedCount:out.blockedCount,reportSha:out.reportSha},null,2));
