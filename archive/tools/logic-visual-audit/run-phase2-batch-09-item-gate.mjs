import fs from 'node:fs';import path from 'node:path';import vm from 'node:vm';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..','..');const OUT=path.join(ROOT,'archive','tools','logic-visual-audit','reports');const d=JSON.parse(fs.readFileSync(path.join(OUT,'phase2_batch_09_c_denominator_frozen.json'),'utf8'));const a=JSON.parse(fs.readFileSync(path.join(OUT,'phase2_batch_09_requirement_adjudication.json'),'utf8'));const sha=v=>`sha256:${crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex')}`;const results=a.entries.map(e=>{const source=e.questionUid.split('|')[0],qid=Number(e.questionUid.split('|').at(-1));const c={window:{}};vm.runInNewContext(fs.readFileSync(path.join(ROOT,source.replaceAll('/',path.sep)),'utf8'),c,{timeout:5000});const q=c.window.questionBank.find(x=>Number(x.id)===qid);const ok=!q.solutionImage&&!q.solutionImageAlt&&!q.solutionImageCaption&&!q.solution.includes('[시각자료 읽기]')&&!q.tags.includes('도형');return{questionUid:e.questionUid,logicVisualItemStatus:ok?'PASS_EXEMPT':'FAIL',solutionImage:q.solutionImage??null,hasVisualReading:q.solution.includes('[시각자료 읽기]'),hasVisualTag:q.tags.includes('도형')};});const out={generatedAtKst:'2026-09-06',phase:'LOGIC_VISUAL_PHASE_2_BATCH_09_ITEM_GATE',status:d.status==='FROZEN'&&d.requiredCount===0&&results.every(r=>r.logicVisualItemStatus==='PASS_EXEMPT')?'PASS_EXEMPT_ITEM_GATE':'FAIL_EXEMPT_ITEM_GATE',denominatorStatus:d.status,requiredCount:d.requiredCount,exemptPassCount:results.filter(r=>r.logicVisualItemStatus==='PASS_EXEMPT').length,results,reportSha:sha(results)};fs.writeFileSync(path.join(OUT,'phase2_batch_09_item_gate.json'),JSON.stringify(out,null,2)+'\n','utf8');console.log(JSON.stringify({status:out.status,requiredCount:out.requiredCount,exemptPassCount:out.exemptPassCount,reportSha:out.reportSha},null,2));if(out.status!=='PASS_EXEMPT_ITEM_GATE')process.exitCode=1;
