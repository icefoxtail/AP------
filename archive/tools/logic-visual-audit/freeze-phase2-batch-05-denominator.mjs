import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..','..');const OUT=path.join(ROOT,'archive','tools','logic-visual-audit','reports');const a=JSON.parse(fs.readFileSync(path.join(OUT,'phase2_batch_05_final_requirement_adjudication.json'),'utf8'));const sha=v=>`sha256:${crypto.createHash('sha256').update(Buffer.isBuffer(v)||v instanceof Uint8Array?v:typeof v==='string'?v:JSON.stringify(v)).digest('hex')}`;const paths={};for(const e of a.entries){if(e.finalVisualRequirement!=='VISUAL_REQUIRED')continue;const parts=e.questionUid.split('|');const exam=path.basename(parts[0],'.js');const q=String(parts[2]).padStart(2,'0');paths[e.questionUid]=`archive/assets/images/${exam}/q${q}-solution.svg`;}const req=Object.fromEntries(a.entries.map(e=>[e.questionUid,e.finalVisualRequirement]));const attached=Object.fromEntries(Object.keys(req).map(u=>[u,req[u]==='VISUAL_REQUIRED']));const falseMap=Object.fromEntries(Object.keys(req).map(u=>[u,false]));const artifactMap=Object.fromEntries(Object.entries(paths).sort(([x],[y])=>x.localeCompare(y)).map(([u,p])=>[u,sha(fs.readFileSync(path.join(ROOT,p.replaceAll('/',path.sep))))]));const maps={finalVisualRequirementMapSha:sha(req),actualSolutionVisualAttachedMapSha:sha(attached),problemVisualMathDependencyMapSha:sha(falseMap),sharedVisualMathDependencyMapSha:sha(falseMap)};const candidateReleaseArtifactSha=sha(artifactMap);const cDenominatorInputSha=sha({...maps,candidateReleaseArtifactSha});const required=a.requiredUidSet;const out={generatedAtKst:'2026-09-06',phase:'LOGIC_VISUAL_PHASE_2_BATCH_05_C_DENOMINATOR',scope:'Pilot-scoped denominator for batch-05 only; q11 is resolved VISUAL_EXEMPT and excluded.',status:'FROZEN',...maps,candidateReleaseArtifactSha,cDenominatorInputSha,logicVisualRequiredUidSet:required,logicVisualRequiredUidSetSha:sha(required),coreFinalCRequiredUidSet:required,coreFinalCRequiredUidSetSha:sha(required),parity:true,requiredCount:required.length,stale:false,denominatorMayBeReusedForGlobalPhase1:false};fs.writeFileSync(path.join(OUT,'phase2_batch_05_c_denominator_frozen.json'),JSON.stringify(out,null,2)+'\n','utf8');console.log(JSON.stringify({status:out.status,requiredCount:out.requiredCount,logicVisualRequiredUidSetSha:out.logicVisualRequiredUidSetSha,cDenominatorInputSha:out.cDenominatorInputSha},null,2));
