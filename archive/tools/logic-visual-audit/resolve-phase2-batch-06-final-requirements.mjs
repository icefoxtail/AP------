import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..','..');const OUT=path.join(ROOT,'archive','tools','logic-visual-audit','reports');const prev=JSON.parse(fs.readFileSync(path.join(OUT,'phase2_batch_06_requirement_adjudication.json'),'utf8'));const sha=v=>`sha256:${crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex')}`;const q18=prev.entries.find(e=>e.questionUid.endsWith('|18'));const entries=prev.entries.map(e=>e.questionUid.endsWith('|18')?{...e,finalVisualRequirement:'VISUAL_REQUIRED',finalVisualType:'SET_GRAPH_DEFINED',visualAction:'KEEP_EXISTING',status:'RESOLVED',reason:'A solutionImage is attached and V2 observed a graph artifact; optional source-only classification cannot bypass required C review when an actual visual is attached.'}:{...e,finalVisualRequirement:'VISUAL_EXEMPT',visualAction:'NONE',status:'RESOLVED'});const required=entries.filter(e=>e.finalVisualRequirement==='VISUAL_REQUIRED').map(e=>e.questionUid).sort();const out={generatedAtKst:'2026-09-06',phase:'LOGIC_VISUAL_PHASE_2_BATCH_06_FINAL_REQUIREMENT_ADJUDICATION',status:'RESOLVED',entries,requiredUidSet:required,pendingCount:0,reportSha:sha(entries)};fs.writeFileSync(path.join(OUT,'phase2_batch_06_final_requirement_adjudication.json'),JSON.stringify(out,null,2)+'\n','utf8');console.log(JSON.stringify({status:out.status,requiredCount:required.length,requiredUidSetSha:sha(required),reportSha:out.reportSha},null,2));
